---
name: kb-bootstrap
description: 私人知识库 + 公众号内容系统的一键搭建器（从零到跑通）。执行 P0 环境检查（系统/Obsidian/浏览器/Web Clipper）、P1 信息采集（身份档案+公众号档案）、P2 三层骨架生成（01_raw/02_wiki/03_output + 配置文件 + 职业定制禁用词库）、P3 指令测试（/ingest、/lint、/newpost）、P4 五大工作流验证（采集/深度调研/发布/PPT/内容创作，含依赖检测与安装）、P5 验收交付。当用户说"搭建知识库""从零建库""一键生成知识库""初始化知识库""kb-setup"，或要为新用户部署整套知识库与公众号系统时使用。日常录入与写稿请用 knowledge-base-dual-engine 技能。
agent_created: true
---

# 知识库一键搭建器（KB Bootstrap）

把一台空白电脑，变成跑得通的「私人知识库 + 公众号内容系统」。
与 `knowledge-base-dual-engine`（日常运营）互补：**本技能负责"建好并测通"，那个负责"日常用起来"**。

## 铁律（贯穿全部阶段）

1. **禁止编造** —— GitHub 地址、软件版本号、用户身份信息，一律实测或追问，绝不猜测。
2. **阻塞即停** —— 装软件、对外发布、删除文件，必须先说明并获用户同意。
3. **每步可验证** —— 每个阶段必须输出"通过 / 不通过 + 证据"，禁止只说"已完成"。
4. **一次一阶段** —— 完成一个阶段向用户汇报，确认后再进入下一个，不跳步。

## 输出结构总览（P2 生成）

```
知识库根/
├── 00-配置/            # 规则层：AGENTS.md、身份档案、公众号档案、风格规范、log.md
├── 01_raw/             # 原始素材层（只读！AI 不许改写原件）
│   └── _inbox/         # Web Clipper 落地点
├── 02_wiki/            # 结构化卡片层
│   ├── 个人库/         # 01-工作 / 02-生活 / 03-学习 / 04-投资（按身份取舍）
│   └── 公众号库/       # 01-选题池 / 02-素材卡片 / 03-金句库 / 04-标题库 / 05-已发布
└── 03_output/          # 成品层：文章 / 方案 / PPT / 公众号 / 图片
```

关键约束：`01_raw` 只读；个人库与公众号库**双库隔离**，互通规则必须写进 AGENTS.md。

---

## P0 环境检查

按序实测每一项，输出检查表（项目 / ✅❌ / 版本或路径 / 待办）：

1. **操作系统** —— Windows 必须区分 10 / 11（决定 opencode 安装方式：Win11 可 winget，Win10 需先确认 PowerShell ≥ 5.1）。
2. **AI 引擎** —— 若正运行于 WorkBuddy，标记"已具备"并跳过 opencode 检查；否则 `opencode --version`，缺失时给出该系统的官方安装命令并**停下等用户确认**。
3. **Obsidian** —— 按系统检测路径（Win: `%LOCALAPPDATA%\Obsidian`；macOS: `/Applications/Obsidian.app`；Linux: `which obsidian`），要求 ≥ 1.5。
4. **浏览器 + Web Clipper** —— 检测 Edge / Chrome；已装则引导：装扩展 → Obsidian 侧启用 Web Clipper 插件 → 复制 API Key → 粘贴到扩展 → **剪藏测试页验证落到 `01_raw/_inbox`**。
5. **工作目录** —— 确认根目录路径、可写、无特殊符号。

**通过标准**：5 项全 ✅，且 Web Clipper 有**真实文件产出**（不是"配置应该没问题"）。

## P1 信息采集

采集两组，一次最多问 3 个问题，答完再问下一组。**信息缺失必须追问，严禁编造或套用通用示例。**

**A 组 · 关于用户**：职业与岗位 / 行业或地域 / 日常产出类型 / 想沉淀的领域（工作·生活·学习·投资，哪些是重点）/ 是否有脱敏场景（体制内·企业 ·医疗教育·无）。

**B 组 · 关于公众号**：号名与一句话定位 / 目标受众（身份 + **痛点**，不止于"年轻人"）/ 内容形态与更新频率 / 素材家底（数量与存放位置）/ 标杆账号（选填）。

产出两个文件：
- `00-配置/身份档案.md` —— 我是谁、我的产出、我要沉淀什么、我的边界、语气偏好
- `00-配置/公众号档案.md` —— 定位、受众画像、内容支柱、更新节奏、调性、禁用内容、与"个人库"的隔离规则

**通过标准**：用户读后说"这确实是我"，而非放之四海皆准的套话。

## P2 骨架生成

严格基于 P1 档案生成，不用通用示例：

1. **三层目录** —— 每个目录放 `README.md`（放什么/不放什么/怎么命名）。**身份里没有的领域不建空目录**（不投资就不建 04-投资）。
2. **`00-配置/AGENTS.md`** —— 三层职责与边界（含"01_raw 只读"铁律）、双库隔离规则、命令定义（/ingest、/lint、/newpost、/query、/选题发现）、脱敏硬性检查项、[[wiki链接]]路径规范。
3. **`00-配置/写作风格规范.md`** —— 分支A（对内）与分支B（对外）语气；**禁用词库按用户职业风险具体生成**（体制内写体制内红线，企业写企业红线），每篇产出强制打 分支A/B 标签。
4. **公众号运营策划** —— 内容支柱 3–5 个（每个配 5 个具体选题示例）、选题 5 维扫描法（时间规律/空间异常/当下关联/矛盾反差/极致对标）、发布节奏、首篇建议。

**通过标准**：目录与身份匹配，禁用词库是**职业定制版**而非通用版。

## P3 指令测试（用真实内容，不许空跑）

每个指令测完输出"通过/不通过 + 证据"；不通过则修复重测，最多 3 轮。

| 指令 | 检查项 |
|------|--------|
| **/ingest** | ①原件未被改写 ②卡片落在 02_wiki **正确**子目录 ③格式合规（一条一事、有来源）④index.md 已更新 ⑤log.md 已追加 |
| **/lint** | 先**人为埋 3 个问题**（两处矛盾说法、指向不存在页面的断链、无反向链接的孤儿页）；检查：①报告含日期 ②**3 个问题全部被抓出** ③P0/P1/P2 分级 ④log.md 已追加 |
| **/newpost** | ①列出相关页面 ②"已确认事实"清单 ③**"待核实"清单**（防编造关键项，为空说明在编造）④素材来源与金句建议 ⑤写作建议（角度/结构/标题备选） |

**通过标准**：三指令全绿，且 /lint 能抓全人为埋的 3 个问题（抓不全 = 体检是摆设，需收紧）。

## P4 五大工作流验证

逐个执行，不要跳过或合并。每工作流循环：**查依赖 → 缺则装 → 已装则真实测试 → 不通则修复重测（最多 3 轮）→ 记录结果**。

| 工作流 | 依赖 | 测试任务 | 通过标准 |
|--------|------|---------|---------|
| **1 内容采集** | Obsidian、Web Clipper、/ingest | 剪藏真实网页 → /ingest → 成卡 | 三步全通，原件 MD5 未变 |
| **2 深度调研** | `wechat-article-search`、`opencli`、`websearch` | 真实选题走 6 步：定目标 → 纵向采集（公众号 15–20 条 + 小红书/知乎/B站）→ 横向采集（对比/趋势/典故）→ 更新 02_wiki → 按需建新页 → 建双链同步 index；产出含调研汇总表 | 覆盖 ≥2 平台、≥3 独立信源；每条事实标出处，单一来源标"待核实"；结果**真正写入 02_wiki 并建立双链**（非只写在对话里）；更新 log.md |
| **3 内容发布** | `humanizer-zh`、`wechat-publish-template`、`baoyu-post-to-wechat`、配图技能 | 走完 10 步：选题(避重扫描) → 调上下文 → 写作(3标题) → 用户确认 → 去AI味 → 爆款评分 → 配图 → 排版 → 发布 → 归档+知识回填 | 爆款评分 ≥85 才允许发布；封面 900×383 / 内图 900×506 共 5–6 张；排版 5 项校验全过；**知识回填已执行**；对外发布前获用户确认 |
| **4 PPT 制作** | PPT 生成能力 | 基于真实内容生成 ~10 页 | 无元素越界、无空白页、每页 ≤200 字、可正常打开（可解析 XML 校验坐标） |
| **5 内容创作** | 公文模板、风格规范、脱敏要求 | 写一份真实场景文稿（总结/汇报/月报） | 符合职业用语；**刻意埋 2 个敏感词验证禁用词库会拦截**；脱敏到位；结构合模板；事实可追溯 |

**依赖缺失处理**：先查官方技能市场/包管理器，再联网搜官方 GitHub 仓库。**严禁编造仓库地址**，找不到就如实报告"未找到，需人工确认"。安装前须告知来源与风险并获同意。

### P4 配套执行清单（详细版 · 建库后日常照做）

P2 阶段要把以下 7 份清单**一并生成到 `00-配置/`**，并在 `AGENTS.md` 写入调用索引。日常执行打开对应清单，不要凭记忆：

| 文件 | 用于 |
|------|------|
| `15-知识卡片模板.md` | 02_wiki 卡片统一 **10 字段**（原文摘要 / 一句话定义 / 基本信息 / 文化内涵 / 关联知识 / 延伸思考 / 情绪钩子 / 双链 / 引用来源 / 变更记录），**强制套用，无模板=不入库** |
| `16-工作流1-内容采集执行清单.md` | 剪藏 → /ingest → 成卡：门禁 G1–G3、9 步、MD5 只读校验 |
| `17-工作流2-深度调研执行清单.md` | 6 步真实信源：公众号纵向 + 多平台 + 权威源，≥3 独立信源 |
| `18-工作流3-公众号发布执行清单.md` | 发布硬门禁 G0–G5 + 10 步闭环 |
| `19-工作流4-PPT制作执行清单.md` | 三条路线做可交付演示稿 |
| `20-工作流5-内容创作执行清单.md` | 文稿模板 + 禁用词扫描 + 脱敏复扫 |
| `21-工作流总纲-技能核查与执行纪律.md` | 五工作流导航 + 全局技能核查表 + 六条铁律 |

**六条执行铁律（违反即返工）**：
1. **技能不偷换**：规定用 `wechat-article-search` 就不用 WebSearch 顶替。正确顺序：**先 `ls` 实测 → 调规定技能 → 失败才兜底 → 兜底必在成果里标注**。
2. **每步可验证**：输出"通过/不通过 + 证据"（MD5、页数、grep 命中数、HTTP 码），禁止只说"已完成"。
3. **门禁优先**：G1–G3 / D1–D5 / G0–G5 / P1–P4 / C1–C4，任一 FAIL 不许往下走。
4. **缺依赖先报后装**：`opencli` / `slidep` / `bun` 缺失 → 给确切安装命令 → 装好再跑；装不上如实标"待装依赖"，**禁止伪造已跑通**。
5. **原件只读**：`01_raw/` 只增不改，ingest 只新建卡片。
6. **回填必做**：每次发布/创作结束，素材沉淀回 `02_wiki`（10 字段卡 + 双链 + index/log），不回填 = 飞轮断链。

## P5 验收交付

1. **对照验收清单逐项打勾**（见工具包 `11-验收测试清单.md`），未通过先修复再交付。
2. **生成专属使用手册** `README-我的知识库.md`（基于实际结果，非通用模板）：目录结构说明、东西往哪放、常用 5 条命令的真实场景示例、禁用词红线摘要、维护节奏、故障速查。
3. **确认飞轮闭环已完整跑通一次**：
   `采集 → /ingest 入库 → /newpost 取料 → 成稿 → 评分/发布 → 沉淀回 02_wiki → /lint 体检 → 继续采集`
4. **输出交付清单**：目录结构、配置文件、已跑通项、已知限制。
5. **给 3 条具体下一步建议**：具体到"做什么、在哪个目录、用什么命令"，禁止"多加练习"这类空话。

## 配图路线 A / B（P4 工作流 3 前置判定，不许临时随机选）

**判定规则**：
1. 判平台：opencode → 路线 A；WorkBuddy → 推荐混用（封面 A + 内图 B），也可全走路线 B。
2. 核实技能真实路径（名字/层级错了会直接跑不通）：
   - `frontend-design` → 项目 `skills/` 或 `.opencode/skills/`
   - `ian-xiaohei-illustrations` → `~/.workbuddy/skills/`（**不在**项目 `skills/` 里）
3. 两者皆无 → 如实报告并降级（纯文字排版 + 微信原生样式），不要为配图卡住发布。
4. 最终采用的路线写进交付说明。

### 路线 A：frontend-design（opencode · 代码驱动设计稿）
产出 HTML 设计稿 → 无头浏览器截图 → PNG。适合封面海报、数据对比图、需精确中文排版的信息图。
1. 定美学方向并写死（极简/杂志编辑风/复古未来/有机自然/工业粗野/奢华精致 选一，贯彻到底，避免通用 AI 审美）
2. 字体禁用 Arial、Inter；配一只有性格的 display 字体 + 克制的正文字体（注意中文商用授权）
3. HTML 尺寸写死：封面 900×383、内图 900×506（截图 deviceScaleFactor=2 保清晰）
4. 文字一律 flex/grid 流式布局，禁止 absolute 定位文字
5. QA：无重叠 / 无溢出 / 无裁切；「HTML 源文字 = 图上文字」
- 优势：改字只需改 HTML 重截，可控可复现
- 劣势：无手绘温度；抽象隐喻（"知识复利""不治理的废墟"）很难画，易变俗气图标

### 路线 B：ian-xiaohei-illustrations（WorkBuddy · 手绘解释图）
`image_gen` 直出 PNG。适合正文认知锚点：核心判断、两个断点、输入输出闭环、分流、前后对比、常见坑、状态变化。
1. 消化正文提炼认知锚点（**不平均配图**，纯抒情段落不配）
2. 先出 shot list：位置/主题/核心意思/结构类型/小黑做什么/元素/中文标注词。默认 4–8 张，短文 1–3 张，长文 ≤9 张
3. 逐张生成（不多图拼一张，每张只讲一个结构），提示词必备 7 要素：
   16:9 横版、纯白背景、黑色手绘线稿、少量红/橙/蓝中文手写批注、大量留白、小黑为核心动作主体、禁止 PPT 感/商业插画/幼稚可爱/复杂架构/左上角类型标题
4. QA 七项（命中即重生成）：小黑只是装饰 / 画面太满 / 像流程图PPT / 中文错字多 / 左上角类型标题 / 画风幼稚死板 / 背景非干净白底
5. 存 `assets/<article-slug>-illustrations/`，命名 `01-xxx.png`，保留原图不覆盖
- 优势：有人味、怪诞有记忆点；抽象隐喻正是它的强项
- 劣势：改字只能整张重生成；出图有随机性
- 降级：平台无图像生成能力（如 opencode 未配置）→ 自动降级路线 A 并告知

### 推荐：混用组合（各管一段，有硬性技术原因）
**封面 ×1 走路线 A；正文内图 4–6 张走路线 B。**
- 封面 900×383 = **2.35:1**，小黑出图固定 **16:9（1.778）**，用作封面必须上下裁切 → 会切掉小黑或批注
- 内图 900×506 = **1.779:1 ≈ 16:9**，与小黑出图几乎吻合 → 可直接使用
- 封面决定点开率，要冲击力与可读大字 → 代码排版更可控
- 内图决定读完率，要耐看不抢戏、有个人 IP → 手绘更合适

**决策速查**：opencode 全走 A ｜ WorkBuddy 推荐混用 ｜ 求稳选 A ｜ 求出彩选 B ｜ 都不可用则降级纯文字。

## 故障速查

| 症状 | 处理 |
|------|------|
| Web Clipper 剪藏失败 | 确认 Obsidian 后台运行；重新复制 API Key；查端口占用 |
| 剪藏文件找不到 | 扩展目标目录设为 `01_raw/_inbox` |
| AI 找不到文件 | 确认 AI 工作区根目录 = 知识库根目录 |
| 中文文件名 404 | 在线访问时对中文路径做百分号编码 |
| /lint 抓不出问题 | 人为埋 3 个问题验证；抓不全则收紧规则 |
| /newpost 无"待核实" | 强制要求输出待核实清单 |
| PPT 元素越界 | 解析 XML 检查坐标是否超画布 |
| **图上文字重叠** | 改用 flex/grid 流式布局，**禁止 absolute 定位文字**；绝对定位只留给图形装饰 |
| **公众号排版丢样式** | 正文 `<p>` 必须显式 `font-size:15px`，不能只挂外层 div（微信会拍平外层样式） |
| **发布报 40164** | 把报错 IP 加入 mp.weixin.qq.com 后台 IP 白名单后重试 |
| 草稿箱多份重复 | 发布后去后台删除旧版同标题草稿，只留最新一份 |
| 技能名找不到 | 注意 `humanizer-zh`（非 humanizer）；`opencli doctor` 可自检 |
| 找不到 ian-xiaohei 技能 | 它在 `~/.workbuddy/skills/`（用户级），不在项目 `skills/` |
| opencode 生不出图 | 平台无图像生成能力 → 路线 B 降级到路线 A |
| 小黑图用作封面被裁 | 16:9 vs 封面 2.35:1 不匹配 → 封面改走路线 A，小黑只做内图 |
| 小黑只是站旁边 | 提示词须写"小黑作为核心动作主体"；命中 QA 就重生成 |
| 小黑图像流程图/PPT | 补全禁忌：禁止 PPT 感、商业插画、幼稚可爱、左上角类型标题 |
| 全局搜索卡死 | 限定目录搜索，不要全库递归 grep |
| AI 编造用户信息 | 回 P1 补全档案，重跑 P2 |

## 内容发布关键约束（P4 工作流 3 必守）

### 发布前硬门禁（缺一即阻断，不许发布）

发布前必须自检以下 5 项，**任一 FAIL 立即停止并引导修复**，不得直接尝试发布：

1. **API 凭证** —— `.baoyu-skills/.env`（项目级 `<cwd>/.baoyu-skills/.env` 或用户级 `~/.baoyu-skills/.env`）含 `WECHAT_APP_ID` 与 `WECHAT_APP_SECRET`。缺失 → 引导到 mp.weixin.qq.com → 开发 → 基本配置 取值并写入，**禁止无凭证发布**。
2. **EXTEND.md 偏好** —— `.baoyu-skills/baoyu-post-to-wechat/EXTEND.md` 存在。缺失会触发该技能 **BLOCKING 首次配置**，后续步骤全部卡死。
3. **IP 白名单** —— 当前出口 IP（`curl -s ifconfig.me`）已加入 mp.weixin.qq.com 白名单，否则报 **40164**。注意家庭宽带 IP 会变，每次发布前复查。
4. **封面 PNG** —— 900×383 的**真实 PNG 文件**（不是 HTML 设计源）。`news` 类型 `thumb_media_id` 必填，无封面发布失败。
5. **接口权限** —— 公众号需有草稿箱/素材管理接口权限；个人未认证号可能无 `draft/add` 权限 → 降级 browser 方式发布。

> 完整自检脚本、首次配置写法、失败速查见 `工作流3-发布执行清单.md`。

**配图**：先按【配图路线 A / B】判定路线再执行。通用要求：
- 封面 `900×383`、内图 `900×506`、内图 4–6 张；命名与 Markdown 中 `![alt](imgs/xx.png)` 一一对应。
- **路线 A**：文字一律走 flex/grid 流式布局，绝对定位只用于图形装饰（实测：absolute 硬排坐标会导致多处文字重叠）。
- 图上文字只放短句（每处 ≤6 字/行、≤4 行），解释性长句留在正文。
- 路线 A 留存 `imgs/_src/*.html` 便于改字重截；路线 B 存原始生成 PNG。

**排版校验（5 项全过才准发布）**：
1. `0` 个 `**` 残留（已转 `<strong>`）
2. 正文 `<p>` 显式 `font-size:15px`
3. 图片为裸 `<img style="display:block;margin:0.5em auto;max-width:100%;height:auto;">`（不用 `figure`）
4. 标题层级 H1=22 / H2=19 / H3=16 px
5. 内图 `<img>` 数量与配图方案一致

**发布纪律**：先 `--dry-run` 验证 → 获用户确认 → 正式发布 → 归档（含 `publish.json`）→ **知识回填必做**（新方法论建卡片、新数据更新对应页、`[[wikilink]]` 互链、同步 README）。

**无图像识别能力时**：图片 QA 以「HTML 源文字 = 图上文字」+ 用户人眼终审为准。

## 配套文件（工具包内）

> 本技能**独立可用**——以下文件会在 P2 阶段由 AI 按你的身份自动生成，无需手动准备；如需现成模板也可从完整工具包获取。

- `09-一键搭建提示词集.md` —— P0–P5 逐条提示词 + Master 总控，可直接复制给任意 AI 客户端
- `10-信息采集表.md` —— P1 输入，用户提前填可省对话轮次
- `11-验收测试清单.md` —— P3/P5 依据，含测试用例与验收标准
- `15-知识卡片模板.md` —— P2 生成于 `00-配置/`，02_wiki 卡片统一 **10 字段**（强制套用，无模板=不入库）
- `16~20-工作流N-*-执行清单.md` —— P2 生成于 `00-配置/`，五份工作流执行清单（采集 / 调研 / 发布 / PPT / 创作），含依赖核查、门禁、分步命令、通过标准、失败速查
- `21-工作流总纲-技能核查与执行纪律.md` —— P2 生成于 `00-配置/`，五工作流导航 + 全局技能核查表 + 六条铁律

## 与日常运营技能的分工

| 场景 | 用哪个 |
|------|--------|
| 从零建库、换机器、给别人部署 | **kb-bootstrap**（本技能） |
| 日常录入、写稿、选题、体检、发布 | **knowledge-base-dual-engine** |
